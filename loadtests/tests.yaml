config:
  target: http://10.152.183.151:8080
  phases:
    - name: "Warm up service"
      duration: 15
      arrivalRate: 5
      rampTo: 20
    - name: "Ramp up high load"
      duration: 100
      arrivalRate: 20
      rampTo: 30
    - name: "Sustained high load"
      duration: 300
      arrivalRate: 30
      rampTo: 40
  ensure:
    maxErrorRate: 1
  payload:
    path: 'mock_data.csv'
    fields:
      - 'userId'
      - 'title'
      - 'description'
      - 'time'
      - 'ingredients'
      - 'steps'
    skipHeader: true


# before:
#   flow:
#     - log: 'Get auth token'
#     - post:
#         url: "/auth/foody/login"
#         json:
#           email: "test@gmail.com"
#           password: "5422"
#         capture:
#           json: "$.token"
#           as: "token"

scenarios:
  - name: "Create new recipe & get recipes"
    flow:
      # - post:
      #     url: "/recipe/1/operations/create"
      #     formData:
      #       title: "{{ title }}"
      #       description: "{{ description }}"
      #       time: "{{ time }}"
      #       steps: "{{ steps }}"
      #       ingredients: "{{ ingredients }}"
      #     headers:
      #       authorization: 'Bearer {{ token }}'
      - get:
          url: "/recipe/view"
